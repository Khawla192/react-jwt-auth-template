import { useContext } from 'react';
import { Link } from 'react-router';

const Landing = () => {

    return (
        <main>
            <h1>Landing Page</h1>
        </main>
    );
};
  
export default Landing;
